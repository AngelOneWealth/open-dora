# GitHub credentials
GITHUB_TOKEN=ghp_your_token_here
GITHUB_ORGS=myorg,anotherorg
GITHUB_REPOS=owner/repo1,owner/repo2  # optional: explicit repos on top of orgs

# DB config is set in docker-compose.yml — no need to duplicate here
